block main
    // Push 1, push 2, add them
    1 2 +
    // Duplicate the result, and print
    // (print consumes one value)
    : print
    // Push 4 (stack: 3 4)
    4
    // Swap (stack: 4 3), sub, print
    ~- print
end
